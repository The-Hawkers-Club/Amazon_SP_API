# Brain dump — estate-intake onboarding

**Status:** RAW — not interrogated yet.

This repo (`AMZN API/Amazon_SP_API`) was discovered as a Git working tree with no `PROJECT-LEDGER.md` and onboarded automatically by `estate-intake.py` (SPEC-006 / PRJ-039), per policy at `~/.claude/state/repo-intake-policy.tsv`. Figure out what this repo is FOR from its own files — README, package manifests, source layout, recent commits — and write the spec that turns it into real, ledgered open items. No further input from him is needed for this seed; that is the point of onboarding by policy rather than by conversation.
